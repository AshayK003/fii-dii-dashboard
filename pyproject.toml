[project]
name = "fii-dii-dashboard"
version = "0.1.0"
description = "NSE FII/DII Data Dashboard — Track institutional investor flows with historical trends"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
